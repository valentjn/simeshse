{
    "devDependencies": {
        "prettier": ">=3.6.2",
        "prettier-plugin-css-order": ">=2.1.2",
        "prettier-plugin-jinja-template": ">=2.1.0",
        "prettier-plugin-organize-attributes": ">=1.0.0",
        "prettier-plugin-toml": ">=2.0.6"
    },
    "scripts": {
        "fmt:check": "npx prettier --check . && uv run ruff format --diff",
        "fmt:misc": "",
        "fmt:prettier": "npx prettier --write .",
        "fmt:ruff": "uv run ruff format",
        "fmt": "npm run fmt:prettier && npm run fmt:ruff && npm run fmt:misc",
        "lint:fix:misc": "uv run python scripts/sync_pyscript.py",
        "lint:fix:ruff": "uv run ruff check --fix",
        "lint:fix": "npm run lint:fix:ruff && npm run lint:fix:misc",
        "lint:misc": "",
        "lint:mypy": "uv run mypy",
        "lint:ruff": "uv run ruff check",
        "lint:ty": "uv run ty check",
        "lint": "npm run lint:mypy && npm run lint:ruff && npm run lint:ty && npm run lint:misc"
    }
}
